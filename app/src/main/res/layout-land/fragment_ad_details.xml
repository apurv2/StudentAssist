<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.apurv.studentassist.AdDetails$PlaceholderFragment" >

    <ImageButton
        android:id="@+id/callposter1"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="#00ff00"
        android:contentDescription="@string/CallButton"
        android:onClick="callPoster"
        android:src="@drawable/ic_action_call" />

    <ImageButton
        android:id="@+id/emailposter"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:background="#000080"
        android:contentDescription="@string/MessageButton"
        android:onClick="emailPoster"
        android:src="@drawable/ic_action_mail" />

</RelativeLayout>